# TagLife - Project Context (سياق المشروع التقني)

## وصف المشروع
**TagLife** هو مشروع تطبيق ويب حديث يتميز بواجهة مستخدم وتجربة مستخدم (UI/UX) عالية الجودة (Premium). الهدف من المشروع هو بناء صفحة هبوط (Landing Page) ومنصة هوية رقمية تعتمد على تقنية NFC، بهوية بصرية متماسكة واحترافية، تتضمن تفاعلات حركية متقدمة، وعروض لنماذج تفاعلية، وملف تعريف رقمي (Digital Profile).

## التقنيات المستخدمة (Tech Stack)
- **إطار العمل الأساسي:** Next.js (الإصدار 16.2.4) باستخدام نظام App Router.
- **مكتبة الواجهات:** React 19.
- **التصميم وتنسيق الصفحات:** Tailwind CSS v4.2.0.
- **الحركات والتفاعلات (Animations):** Framer Motion و `tw-animate-css`.
- **مكونات الواجهة (UI Components):** Radix UI (مكونات الواجهة القابلة للوصول، تُستخدم مع نمط Shadcn UI).
- **الأيقونات:** Lucide React.
- **لغة البرمجة:** TypeScript.
- **إدارة النماذج والتحقق:** React Hook Form مع Zod.

## هيكل الملفات والمجلدات الرئيسية (Folder Structure)

المشروع مبني على الهيكلية القياسية لـ Next.js ويحتوي على الأقسام التالية:

### 1. مجلد `app/`
يحتوي على الملفات الأساسية للتوجيه (Routing) في Next.js:
- `page.tsx`: الصفحة الرئيسية (Landing Page) التي تدمج جميع الأقسام.
- `layout.tsx`: التخطيط الأساسي (Root Layout) الذي يغلف التطبيق بأكمله ويحتوي على مزودات الحالة والخطوط.
- `globals.css`: التنسيقات العامة للمشروع.

### 2. مجلد `components/`
يحتوي على جميع مكونات React، وينقسم بشكل رئيسي إلى:
- **`components/landing/`**: المكونات الخاصة بأقسام صفحة الهبوط، وتشمل:
  - `hero.tsx`: القسم العلوي والافتتاحي للصفحة (Hero Section).
  - `card-showcase.tsx`: قسم استعراض البطاقات الخاصة بالمنصة.
  - `trusted-by.tsx`: قسم العلامات التجارية الموثوقة (يحتوي على شريط تمرير لانهائي).
  - `digital-profile.tsx`: قسم يعرض ميزات الملف التعريفي الرقمي الخاص بالمستخدمين.
  - `final-cta.tsx`: قسم الدعوة لاتخاذ إجراء في نهاية الصفحة (Call to Action).
- **`components/ui/`**: المكونات الأساسية للواجهة (مثل الأزرار، القوائم، الحوارات) المبنية فوق Radix UI لتكون قابلة لإعادة الاستخدام في كل المشروع.
- `theme-provider.tsx`: مكون لإدارة السمة (الوضع الليلي والنهاري) للتطبيق.

### 3. مجلدات أخرى هامة
- `lib/`: مجلد يحتوي على دوال مساعدة (Utility functions) مثل دالة دمج كلاسات Tailwind `cn`.
- `hooks/`: مجلد للخطافات (Custom React Hooks) التي تدير حالات معينة في التطبيق.
- `public/`: مجلد يحتوي على الملفات الثابتة والصور (مثل شعارات الشركات، صور النماذج).
- `styles/`: مجلد التنسيقات الإضافية.

### 4. ملفات الإعداد والتكوين (Configuration Files)
- `package.json`: يحتوي على تفاصيل المشروع، أوامر التشغيل (Scripts)، وحزم NPM المثبتة (Dependencies).
- `theme.css` و `variables.css`: ملفات تحتوي على متغيرات الألوان (CSS Variables) ونظام التصميم الخاص بالهوية البصرية للمشروع.
- `next.config.mjs`: إعدادات Next.js.
- `components.json`: ملف إعدادات يعود لأداة إضافة مكونات الواجهة (Shadcn UI).
- `tsconfig.json`: إعدادات لغة TypeScript.
- `tokens.json` و `svgs.json`: ملفات تحتوي على بيانات التصميم الثابتة (Design Tokens) وبيانات الأيقونات.
